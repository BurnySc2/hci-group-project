import React, { Component } from "react"

export default class Groupfinder extends Component {
    // TODO sortable table with all groups that you are not part of
    // filter and search function
    // request to join group function
    render() {
        return <div></div>
    }
}
